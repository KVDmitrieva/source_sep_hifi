train:
  datasets:
    - _target_: src.datasets.VCTKDataset
      part: testset
      data_dir: /kaggle/input/vctk-datasets
      max_audio_length: 32768
      limit: ${dataloaders.batch_size}
val:
  datasets:
    - _target_: src.datasets.VCTKDataset
      part: testset
      data_dir: /kaggle/input/vctk-datasets
      max_audio_length: 32768
      limit: ${dataloaders.batch_size}
